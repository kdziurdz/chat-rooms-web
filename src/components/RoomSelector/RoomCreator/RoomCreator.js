import React from 'react';
import {Button, Navbar} from "react-bootstrap";

function RoomCreator() {
    return (
            <Button variant="secondary" block>Create</Button>
    );
}

export default RoomCreator;